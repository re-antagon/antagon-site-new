{
  "packageManager": "npm@10.2.4",
  "devDependencies": {
    "@vite-pwa/vitepress": "^1.0.0",
    "shiki": "^3.11.0",
    "sitemap-ts": "^1.10.1",
    "vite-plugin-pwa": "^1.0.3",
    "vite-plugin-sitemap": "^0.8.2",
    "vitepress": "^2.0.0-alpha.15"
  },
  "scripts": {
    "docs:dev": "npx vitepress dev docs",
    "docs:build": "npx vitepress build docs && npm run generate:sitemap",
    "docs:preview": "npx vitepress preview docs",
    "generate:sitemap": "node -e \"require('./docs/.vitepress/generate-sitemap.js').generateSiteMap().catch(console.error)\""
  },
  "dependencies": {
    "sitemap": "^8.0.0",
    "vue": "^3.5.20"
  }
}
